;;;; /**
;;;;  * Demultiplexor:
;;;;  * {a, b} = {in, 0} if sel == 0
;;;;  *          {0, in} if sel == 1
;;;;  */

(defgate DMux
    (IN
        in
        sel)

    (OUT
        a
        b)

  (And (a in) (b sel) (out b))
  (Not (in sel) (out notsel))
  (And (a in) (b notsel) (out a)))

